# Read by the `alohomora.publish` convention plugin, which otherwise falls back to a generic
# description shared by every module.
alohomora.pom.description=Gradle plugin that injects Git and build metadata into Alohomora debug builds
